#name of workflow 
name: github action program
#define trigger
on: 
  push:
    branches:
      - main

#define jobs
jobs: 
  prctice_actions:
    runs-on: ubuntu-latest
    steps:
    - name: checkout code
      uses: actions/checkout@v2
    - name: show python version
      run: python --version
    - name: print welcome message
      run: echo "welcome to avd"


